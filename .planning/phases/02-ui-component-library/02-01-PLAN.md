---
phase: 02-ui-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - front-end/src/components/ui/button.tsx
  - front-end/src/components/ui/card.tsx
  - front-end/src/components/ui/input.tsx
  - front-end/src/components/ui/dialog.tsx
  - front-end/src/components/ui/dropdown-menu.tsx
  - front-end/src/components/ui/badge.tsx
  - front-end/src/components/ui/skeleton.tsx
  - front-end/src/components/ui/sonner.tsx
  - front-end/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "8 shadcn primitives are installed and importable"
    - "Sonner toasts can be triggered from any component"
    - "Components use CSS variables for theming"
  artifacts:
    - path: "front-end/src/components/ui/button.tsx"
      provides: "Button component with variants"
      contains: "buttonVariants"
    - path: "front-end/src/components/ui/card.tsx"
      provides: "Card component with subcomponents"
      contains: "CardHeader"
    - path: "front-end/src/components/ui/input.tsx"
      provides: "Input component"
      contains: "Input"
    - path: "front-end/src/components/ui/dialog.tsx"
      provides: "Dialog component with subcomponents"
      contains: "DialogContent"
    - path: "front-end/src/components/ui/dropdown-menu.tsx"
      provides: "Dropdown menu with subcomponents"
      contains: "DropdownMenuContent"
    - path: "front-end/src/components/ui/badge.tsx"
      provides: "Badge component with variants"
      contains: "badgeVariants"
    - path: "front-end/src/components/ui/skeleton.tsx"
      provides: "Skeleton loading component"
      contains: "Skeleton"
    - path: "front-end/src/components/ui/sonner.tsx"
      provides: "Toast notification component"
      contains: "Toaster"
  key_links:
    - from: "front-end/src/app/layout.tsx"
      to: "@/components/ui/sonner"
      via: "Toaster import and render"
      pattern: "<Toaster"
---

<objective>
Install shadcn/ui primitive components and configure toast notifications.

Purpose: Provide the foundational UI building blocks that feature components (Navbar, Counter Display, Network Indicator) will compose in subsequent plans.

Output: 8 shadcn components in `components/ui/`, Toaster integrated into root layout for app-wide toast support.
</objective>

<execution_context>
@/Users/kenny/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kenny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-component-library/02-RESEARCH.md

# Phase 1 established shadcn config
@front-end/components.json
@front-end/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui primitives via CLI</name>
  <files>
    front-end/src/components/ui/button.tsx
    front-end/src/components/ui/card.tsx
    front-end/src/components/ui/input.tsx
    front-end/src/components/ui/dialog.tsx
    front-end/src/components/ui/dropdown-menu.tsx
    front-end/src/components/ui/badge.tsx
    front-end/src/components/ui/skeleton.tsx
    front-end/src/components/ui/sonner.tsx
  </files>
  <action>
    Run shadcn CLI to install all 8 required components:

    ```bash
    cd front-end && npx shadcn@latest add button card input dialog dropdown-menu badge skeleton sonner --yes
    ```

    The CLI will:
    - Create component files in src/components/ui/
    - Auto-install required dependencies (@radix-ui/*, sonner, class-variance-authority, tailwindcss-animate)
    - Use existing components.json configuration (new-york style, cssVariables: true)

    After installation, verify each component file exists and exports its main component.

    Note: The --yes flag auto-accepts prompts. If Button already exists, CLI will skip or overwrite - either is fine.
  </action>
  <verify>
    1. Run: `ls front-end/src/components/ui/` - should show all 8 .tsx files
    2. Run: `cd front-end && pnpm build` - should complete without errors
    3. Check each file exports expected components (Button, Card, CardHeader, Input, Dialog, DropdownMenu, Badge, Skeleton, Toaster)
  </verify>
  <done>
    All 8 shadcn primitives exist in components/ui/ and the project builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Toaster to root layout</name>
  <files>front-end/src/app/layout.tsx</files>
  <action>
    Update the root layout to include the Sonner Toaster component:

    1. Import Toaster from "@/components/ui/sonner"
    2. Add `<Toaster />` inside the body, after {children} but before closing AppProviders
    3. Configure with sensible defaults:
       - `richColors` for success/error styling
       - `position="top-right"` for standard notification placement

    The Toaster should be placed at the end of the body content so it renders on top of other elements.

    Example placement:
    ```tsx
    <AppProviders>
      {children}
      <Toaster richColors position="top-right" />
    </AppProviders>
    ```
  </action>
  <verify>
    1. Run: `grep -n "Toaster" front-end/src/app/layout.tsx` - should show import and usage
    2. Run: `cd front-end && pnpm build` - should complete without errors
  </verify>
  <done>
    Toaster component is imported and rendered in root layout. Toast notifications can now be triggered from any component using `import { toast } from "sonner"`.
  </done>
</task>

</tasks>

<verification>
1. All 8 component files exist: `ls front-end/src/components/ui/*.tsx | wc -l` equals 8
2. Build succeeds: `cd front-end && pnpm build` exits 0
3. Toaster in layout: `grep "Toaster" front-end/src/app/layout.tsx` shows import and JSX
4. Dependencies installed: Check package.json has @radix-ui packages, sonner, class-variance-authority
</verification>

<success_criteria>
- 8 shadcn primitive components installed and importable
- Project builds without errors
- Toaster integrated in layout for app-wide toast support
- All components use CSS variables (automatic via cssVariables: true config)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-component-library/02-01-SUMMARY.md`
</output>
