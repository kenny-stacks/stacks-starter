---
phase: 02-ui-component-library
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - front-end/src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Main page displays Navbar at top"
    - "Main page shows Counter Display with mock value"
    - "Main page shows Network Indicator with devnet"
    - "Dark/light mode works across all components"
  artifacts:
    - path: "front-end/src/app/page.tsx"
      provides: "Main page composing all feature components"
      contains: "Navbar"
  key_links:
    - from: "front-end/src/app/page.tsx"
      to: "@/components/navbar"
      via: "Navbar import and render"
      pattern: "import.*Navbar"
    - from: "front-end/src/app/page.tsx"
      to: "@/components/counter-display"
      via: "CounterDisplay import and render"
      pattern: "import.*CounterDisplay"
    - from: "front-end/src/app/page.tsx"
      to: "@/components/network-indicator"
      via: "NetworkIndicator import and render"
      pattern: "import.*NetworkIndicator"
---

<objective>
Integrate feature components into the main page and verify the complete UI.

Purpose: Bring together all Phase 2 components into a cohesive developer experience. This is what developers see when they first run the starter kit.

Output: Working main page with Navbar, Counter Display, and Network Indicator. Visual verification of complete UI.
</objective>

<execution_context>
@/Users/kenny/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kenny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ui-component-library/02-RESEARCH.md

# Prior plan context
@.planning/phases/02-ui-component-library/02-01-SUMMARY.md
@.planning/phases/02-ui-component-library/02-02-SUMMARY.md

# Feature components to integrate
@front-end/src/components/navbar.tsx
@front-end/src/components/counter-display.tsx
@front-end/src/components/network-indicator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main page with feature components</name>
  <files>front-end/src/app/page.tsx</files>
  <action>
    Replace the temporary Phase 1 homepage with the full Phase 2 UI:

    1. Import all feature components (Navbar, CounterDisplay, NetworkIndicator)
    2. Layout structure:
       - Navbar at top (outside main content area - it's sticky)
       - Main content area with centered layout
       - NetworkIndicator showing current network (hardcoded "devnet" for now)
       - CounterDisplay centered on page

    ```tsx
    import { Navbar } from "@/components/navbar"
    import { CounterDisplay } from "@/components/counter-display"
    import { NetworkIndicator } from "@/components/network-indicator"

    export default function Home() {
      return (
        <div className="min-h-screen bg-background">
          <Navbar />
          <main className="container py-8">
            <div className="flex flex-col items-center gap-6">
              <div className="flex items-center gap-2">
                <span className="text-sm text-muted-foreground">Network:</span>
                <NetworkIndicator network="devnet" />
              </div>
              <CounterDisplay value={0} />
            </div>
          </main>
        </div>
      )
    }
    ```

    Note: Network is hardcoded to "devnet" for now. Phase 3 will make this dynamic based on wallet connection.

    The page should NOT have "use client" directive - it can be a Server Component since individual components handle their own client requirements.
  </action>
  <verify>
    1. Run: `grep "import.*Navbar" front-end/src/app/page.tsx` - should show import
    2. Run: `grep "import.*CounterDisplay" front-end/src/app/page.tsx` - should show import
    3. Run: `grep "import.*NetworkIndicator" front-end/src/app/page.tsx` - should show import
    4. Run: `cd front-end && pnpm build` - should succeed
    5. Run: `cd front-end && pnpm dev` - should start without errors
  </verify>
  <done>
    Main page integrates Navbar, CounterDisplay, and NetworkIndicator. Page renders with all Phase 2 components.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 2 UI with:
    - Navbar (logo, theme toggle, placeholder wallet button)
    - Network indicator (showing "Devnet")
    - Counter display (showing 0, with disabled increment/decrement buttons)
    - Dark/light mode theming
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd front-end && pnpm dev`
    2. Open http://localhost:3000 in browser

    **Verify Navbar:**
    - "Stacks Starter" logo/text visible on left
    - Theme toggle button (sun/moon icon) on right
    - "Connect Wallet" button on right (should be clickable but do nothing)

    **Verify Network Indicator:**
    - Shows "Devnet" badge below navbar
    - Badge has outline/neutral styling

    **Verify Counter Display:**
    - Card with "Counter Value" title
    - Shows "0" as the value in large text
    - "- Decrement" and "+ Increment" buttons visible but disabled (grayed out)

    **Verify Dark Mode:**
    - Click theme toggle - entire UI should switch between light/dark
    - Navbar, cards, badges, buttons should all respect theme
    - No flicker or hydration issues

    **Verify Responsive:**
    - Resize browser to mobile width
    - Layout should remain functional (no overflow, buttons accessible)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. All imports present in page.tsx
2. Build succeeds without errors
3. Dev server starts without errors
4. Visual verification of all 5 Phase 2 success criteria:
   - Navbar renders with placeholder wallet button
   - Counter display shows mock value (0)
   - Network indicator displays current network
   - All 8 shadcn primitives render correctly
   - UI adapts to dark/light mode
</verification>

<success_criteria>
- Main page displays all feature components
- Navbar, Counter Display, and Network Indicator render correctly
- Dark/light mode toggle works across all components
- No TypeScript or build errors
- Visual appearance matches expectations
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-component-library/02-03-SUMMARY.md`
</output>
